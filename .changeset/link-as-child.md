---
"@jasonruesch/react": minor
---

Add an `asChild` prop to `Link`, letting consumers render a third-party link component (such as React Router's `Link`) as the base while keeping the design-system styling.
