---
"@jasonruesch/react": patch
---

Use the shared `Spinner` component for `Button`'s loading state instead of a duplicate inline SVG, so the spinner stays consistent with the rest of the design system.
