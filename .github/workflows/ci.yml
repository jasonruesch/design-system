name: CI

on:
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

# CI runs lint/typecheck/test as explicit steps; husky's local git hooks add no
# value here, so skip installing them.
env:
  HUSKY: "0"

jobs:
  check:
    name: Format, Lint, Typecheck, Test & Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Build first so dist artifacts (e.g. @jasonruesch/tokens/css, consumed by
      # the Tailwind stylesheet) exist before the checks that resolve them.
      - name: Build
        run: pnpm build

      - name: Format check
        run: pnpm format:check

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      # The docs Storybook stories run as Vitest browser tests via Playwright.
      - name: Install Playwright browser
        run: pnpm --filter @jasonruesch/docs exec playwright install --with-deps chromium

      - name: Test
        run: pnpm test
